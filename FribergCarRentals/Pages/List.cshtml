@page
@using FribergCarRentals.Components
@model ListModel

@{
    var type = Model.Type;
    var user = Model.UserType;
    ViewData["Title"] = Model.Type;
}

    @await Component.InvokeAsync(nameof(ListAll), new { type = type, user = user })
